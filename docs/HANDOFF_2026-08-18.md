# Price Analyzer 인수인계 — 2026-08-18

## 이번 변경의 목적

이번 변경은 표준 DB의 근거 수를 실제 독립 원본 기준으로 설명하고, 비교하기 어려운 품목에
가격을 억지로 붙이지 않으며, 구매팀이 실제 협상에 사용할 수 있는 `협상 가능금액`을 명확히
제시하는 데 목적이 있다. 임원 발표자료도 같은 원칙으로 구성했다.

## 최신 작업 위치와 실행 상태

- 작업 경로:
  `C:\Users\sangwoo\Documents\Price-analyzer\.worktrees\local-data-foundation`
- 운영 DB: `backend/.local/standard-item-migration-v2.sqlite3`
- DB 스키마: Alembic `0014`
- 표준 품목: `7,684개`
- 최신 운영 투영: `ACTIVE 7,684개`, 가격 연결 누락 `0개`
- 정제 최신 판정: 포함 `24,459건`, 제외 `12,473건`, 검토 대기 `8,336건`
- 적용 전 백업:
  `backend/.local/backups/standard-item-migration-v2.before-executive-20260818-002321.sqlite3`
- 백업 SHA-256:
  `3CB83704E0DD770CE324E09C859DFC8ABC7EE682102618F61E8EA2E1E9CA84F9`
- 적용 DB SHA-256:
  `5A8DECAB37976FAAEFF32E4B45B38685CBC43D71D4511CB5D64F5A56BEAA3C58`
- 무결성 검사: `integrity_check=ok`, 외래키 오류 `0건`

## 구매 판단과 협상 목표의 현재 원칙

### 가격 적정성

- 과거 견적의 최저·중앙값·최고값을 함께 보여준다.
- 기본 임계치는 중앙값 대비 `±10% 적정`, `±10~20% 주의`, `±20% 초과 고가·저가`다.
- 규격이 비어 있고 가격 범위가 2배를 넘는 그룹은 자동 판정하지 않는다.
- 표준 DB가 없을 때만 시장가를 확인하며, 정확한 모델번호가 없는 제작품·가공품은 외부
  유사 상품을 자동 근거로 사용하지 않는다.

### 구매 목표가와 협상 가능금액

- 원본 견적일이 확인된 독립 원본의 과거 개당 단가를 KOSIS 소비자물가 총지수 등락률로
  현재 가치에 보정한다.
- 같은 문서의 복사본은 한 번만 계산하고, 보정된 단가 중 최저값을 협상 목표 단가로 사용한다.
- 품목별 협상 가능금액은 현재 구매금액이 목표금액보다 높은 경우만 합산한다.
- 이미 목표가보다 싸게 들어온 품목은 협상 가능금액 `0원`으로 두며 다른 품목의 여력을
  상쇄하지 않는다.
- 전체 협상 목표금액은 `전체 견적금액 - 협상 가능금액`이다.
- 근거가 부족한 품목은 현재 구매금액을 유지하며 목표가를 만들어내지 않는다.

## 화면 변경

- 표준 DB 근거 품질은 `제출사 확인 필요`, `견적 제출사 1곳`, `견적 제출사 2곳 이상`으로
  표시한다.
- 신규 견적 분석의 참조 가격은 중앙값과 일치하도록 정리하고 최저·기준·최고를 구분했다.
- 구매 목표가 화면에는 협상 가능금액과 전체 협상 목표금액만 강조하며, 음수 차액은 합산하지
  않는다.
- 표준 DB 검색과 근거 표시, 정제 검토 화면의 가독성을 보완했다.

## 데이터 갱신 순서

원본 견적이 바뀌거나 파싱·정제 규칙을 갱신한 경우 저장소 루트에서 다음 명령을 사용한다.

```bat
scripts\start-local.bat --refresh-data
```

이 옵션은 원본 증분 적재 후 최신 정제 규칙으로 전수 재평가하고 표준 DB를 다시 만든다.
운영 DB를 직접 덮어쓰기 전에 반드시 `backend/.local/backups/`에 복사본을 남긴다.

수동으로 확인할 때는 다음 순서다.

```bat
cd backend
..\.venv\Scripts\python.exe -m alembic upgrade head
..\.venv\Scripts\python.exe -m app.cli ingest --quote-root ..\견적서 --database-file .local\standard-item-migration-v2.sqlite3 --report .local\reports\latest-ingestion.json --json
..\.venv\Scripts\python.exe -m app.cleansing.reassess
..\.venv\Scripts\python.exe -m app.cli standard-db-build --database-file .local\standard-item-migration-v2.sqlite3 --report .local\reports\latest-standard-db.json --json --actor local-operator
```

같은 원본과 같은 규칙으로 재실행했을 때 새 파싱 행·판정·표준가격이 중복 생성되지 않는지
확인한다.

## 검증 결과

- 백엔드 전체 테스트: `707 passed, 7 skipped`
- 프런트엔드 전체 테스트: `58 passed`
- 프런트 lint 및 프로덕션 빌드 통과
- 임원 발표자료 6장 전체 렌더 확인 및 화면 밖 요소 검사 통과
- 시연용 32품목 견적 분석:
  - 표준 DB 매칭 `24개`
  - 시장가 확인 `2개`
  - 판정 대기 `8개`
  - 구매 목표가 산정 `16개`
  - 시연 협상 가능금액 `984,279원`
  - 시연 전체 협상 목표금액 `27,670,921원`

시연 수치는 제품 기능 설명용이며 실제 협상 성과를 보장하는 수치가 아니다.

## 임원 발표자료

- 파일: `output/Price_Analyzer_임원발표_2026-08-18.pptx`
- 6장 구성: 목적, 처리 흐름, 협상 목표가 산정 원칙, 신뢰 장치, 한계와 활용 방향
- 모든 웹 화면 캡처는 화이트 모드로 통일했다.
- 표와 도형은 PowerPoint에서 직접 수정할 수 있다.
- 발표자료 안의 수치는 32품목 시연 견적의 실제 로컬 분석 결과를 사용했다.

## 남은 한계와 다음 우선순위

1. 원본에서 규격·제품 제조사·견적일이 누락된 품목은 완전 자동 비교가 어렵다.
2. 모델번호가 없는 제작품·가공품은 DeviceMart·Mouser 검색만으로 신뢰할 수 있는 시장가를
   확보하기 어렵다.
3. 보안 파일은 사내 PC에서 보안해제본을 추가한 뒤 동일 갱신 절차로 다시 처리해야 한다.
4. 검토 완료 결과를 다시 표준 DB에 축적하는 운영 절차와 담당자 권한은 사내 운영 단계에서
   확정해야 한다.
5. 사내망 hChat과 Mouser 키는 사내 환경에서 별도 확인한다.

이 시스템은 자동 승인 시스템이 아니다. 근거가 있는 품목의 협상 우선순위를 빠르게 찾고,
근거가 약한 품목은 원본과 함께 구매 담당자에게 돌려주는 의사결정 지원 도구다.
